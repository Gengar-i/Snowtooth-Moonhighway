import { CircularProgress } from "@mui/material";

const Loader = () => {
  return (
    <div className="sm-container">
      <CircularProgress className="sm-loader" />
    </div>
  );
};

export default Loader;
